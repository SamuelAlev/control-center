/// Leaf library for [CcInfraLog] — the Flutter-free logging seam.
///
/// Import this instead of the full `package:cc_infra/cc_infra.dart` barrel
/// when the caller only needs to install a log sink.
library;

export 'src/log/cc_infra_log.dart';
