/// Leaf library for `CcPaths` — the on-disk layout resolver.
///
/// Import this instead of `package:cc_infra/cc_infra.dart` when the caller
/// only needs path layout.
library;

export 'src/util/cc_paths.dart';
