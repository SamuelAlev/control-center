/// Shared-kernel GitHub profile types. Presentation imports them from
/// `cc_domain` directly; this re-export keeps the GraphQL client on one path.
library;

export 'package:cc_domain/core/domain/entities/github_user_profile.dart';
