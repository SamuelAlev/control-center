/// System-prompt block for chat mode.
///
/// Chat is the default free-for-all mode — no extra constraints injected
/// into the system prompt. The agent's own persona/system-prompt remains
/// the sole behavior driver.
String buildChatModePrompt() => '';
