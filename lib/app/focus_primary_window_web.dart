/// Web: there is no native multi-window layer (and no floating focus-pill HUD),
/// so bringing the primary window to the front is a no-op. The browser tab is
/// already the only surface.
void focusPrimaryWindow() {}
