# Build artifacts
.dart_tool/
build/

# Not needed to compile the binary (pubspec is synthesized in the builder)
pubspec.yaml
pubspec.lock
analysis_options.yaml

# Sources not required by the AOT compile
test/
README.md

# Docker self-references
Dockerfile
.dockerignore

# OS / editor noise
.DS_Store
*.swp
